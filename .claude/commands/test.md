Run the integration testing skill against running local dev servers.

Arg (optional): scenario name to focus on (e.g., `dm-basic`, `dm-invite`, `dm-group`, `voting`, `voting-finals`, `lifecycle`, `chat`, `demo`, `cast`, `economy`). No arg = full test plan.

Read and follow the full skill at `~/.claude/skills/integration-testing/SKILL.md`.

Scenario to run: $ARGUMENTS
